# Dzeroth — Production environment variable template
#
# Copy this file to deploy/secrets/env.production and fill in all values.
# deploy/secrets/ is in .gitignore — NEVER commit secrets.
#
# All blank values marked REQUIRED must be provided or the API will refuse to start.

# ── PostgreSQL ────────────────────────────────────────────────────────────────
POSTGRES_USER=dzeroth
POSTGRES_DB=dzeroth
POSTGRES_PASSWORD=REQUIRED_change_this_strong_password

# IMPORTANT: Choose based on your PostgreSQL deployment type:
#
#   Managed PostgreSQL (AWS RDS, Cloud SQL, Supabase, Neon — RECOMMENDED):
#     POSTGRES_SSL_MODE=require        — TLS required, CA from system pool
#     POSTGRES_SSL_MODE=verify-full    — TLS required + hostname verification
#     Both are correct for managed services. 'require' is the minimum acceptable.
#
#   Bundled Compose postgres service (docker-compose.prod.yml postgres:):
#     POSTGRES_SSL_MODE=disable        — The bundled image has no TLS configured.
#     Using 'require' against the bundled service WILL CAUSE A STARTUP FAILURE.
#     To use TLS with the bundled service, mount server certificates and set
#     ssl=on in a custom postgresql.conf (see PostgreSQL TLS documentation).
#
# 'disable' is NOT acceptable when connecting to a managed PostgreSQL service.
POSTGRES_SSL_MODE=require

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=REQUIRED_change_this_strong_password

# Set true when using managed Redis with TLS (Redis Cloud, ElastiCache TLS mode, etc).
REDIS_TLS=false

# ── JWT ───────────────────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 48
# Must be at least 32 bytes. Rotation requires all existing sessions to re-authenticate.
JWT_SECRET=REQUIRED_generate_with_openssl_rand_base64_48

# ── Server ────────────────────────────────────────────────────────────────────
API_PORT=8080

# Admin/observability server. Do NOT publish this port on the public load balancer.
# In production this port is intentionally not published (see docker-compose.prod.yml).
ADMIN_ADDR=:9091

# ── CORS ──────────────────────────────────────────────────────────────────────
# Exact origins the API will accept cross-origin requests from.
# Must match the exact scheme+host+port of your deployed Flutter web app or CDN origin.
# Multiple origins are comma-separated.
# Example: CORS_ALLOWED_ORIGINS=https://app.dzeroth.com
CORS_ALLOWED_ORIGINS=REQUIRED_https://your-frontend-domain.com

# ── Logging ───────────────────────────────────────────────────────────────────
LOG_LEVEL=info

# ── Session cleanup ───────────────────────────────────────────────────────────
# How often the background worker removes expired session rows from PostgreSQL.
SESSION_CLEANUP_INTERVAL=1h

# ── Title worker intervals (optional — defaults shown) ────────────────────────
# How often the title qualification worker runs a full reconciliation pass.
TITLE_WORKER_INTERVAL=5m
# How often the title notification worker dispatches pending title notifications.
TITLE_NOTIFICATION_INTERVAL=1m
